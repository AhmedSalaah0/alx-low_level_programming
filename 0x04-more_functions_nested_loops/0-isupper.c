#include "main.h"

/**
 * _isupper(c) is a function to check if the char is in upper case
 * if upper return 1, otherwise 0
 *
 */

int _isupper(c)
{
if (c >= 60 && c <= 90)
{
	return (1);
}
else 
	return (0);
}
